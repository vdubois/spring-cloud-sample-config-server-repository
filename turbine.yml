info:
  component: Turbine
turbine:
  clusterNameExpression: new String("default")
  appConfig: microservice,recommendations-service
server:
  port: 8989
management:
  port: 8990
eureka:
  client.serviceUrl.defaultZone: http://localhost:9000/eureka
  instance:
    app-group-name: microservices
    hostname: localhost
    appname: turbine
